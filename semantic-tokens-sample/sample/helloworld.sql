-- hello world
&& Hello world
select * from table_name
{# hello world #}
// hello world

/* Hello World */

SELECT
  *
IF
  (x.y IS NULL,
    FALSE,
    TRUE) AS z,
  {# Comment A #}
  {# Comment B #}
  1 AS foo,
  {# Instead of <img> we match for the <img> pattern since that's how it appears in the post body #}
  2 AS bar,
  3 AS baz
FROM
  table